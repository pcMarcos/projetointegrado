<div class="container-fluid">
  <div class="row">
    <div class="col my-4">
      <%= link_to 'Voltar à página inicial', root_path, class: 'btn btn-outline-dark'  %>
      <%= link_to 'Crie um nova despesa', new_expense_path, class: 'btn btn-outline-dark'  %>
      <%= link_to 'Voltar aos limites', limits_url, class: 'btn btn-outline-dark'  %>
    </div>
  </div>
</div>

<div class="container">
  <div class="row">
    <div class="col-4">
      <h3> Criar Limite </h3>
      <%= form_with(model: @limit, url: limits_path) do |f| %>
        <div class="card">
          <div class="card-body">
            <div class="mb-3">
              <%= f.label :nome, { class: "form-label"} %>
              <%= f.text_field :name, { class: "form-control", placeholder: "Nome do limite" } %>
            </div>
            <div class="mb-3">
              <%= f.label :valor, { class: "form-label"} %>
              <%= f.text_field :value, { class: "form-control", placeholder: "Valor do limite" } %>
            </div>
            <div class="mb-3">
              <%= f.label :categoria, { class: "form-label"} %>
              <%= f.select :category_id, options_from_collection_for_select(@categories, :id, :name), {}, { class: "form form-group form-control" } %>
            </div>
            <%= f.submit "Criar limite", class: "btn btn-outline-dark" %>
          </div>
        </div>
      <% end %>
    </div>
  </div>
</div>

